<div class='container'>
  <h1>Trânsito SP</h1>

  <% @per_zone_traffic.each do |zone, value| %>
    <section>
      <h2 class='airport-panel'><%= zone_name_for(zone) %></h2>
      <p class='airport-panel'><%= value %> km</p>
    </section>
  <% end %>

  <section>
    <h2 class='airport-panel'>Total</h2>
    <p class='airport-panel'><%= @overall_traffic %> km</p>
  </section>
</div>